<section class="max-w-xl space-y-5"><div><p class="text-sm font-semibold text-sky-800">Administration</p><h1 class="text-3xl font-bold">Sessionize</h1></div><div class="space-y-4 rounded-xl border border-slate-200 bg-white p-4"><div><h2 class="font-bold">Configuration</h2><p class="mt-1 text-sm">{{ $configured ? 'Configured' : 'Not configured' }}</p></div><div><h2 class="font-bold">Latest sync</h2>@if ($lastSyncRun)<p class="mt-1 text-sm">{{ $lastSyncRun->status->value }} · {{ $lastSyncRun->created_at->format('D M Y H:i') }}</p>@else<p class="mt-1 text-sm">No synchronization has run yet.</p>@endif</div><button type="button" disabled class="min-h-11 cursor-not-allowed rounded-md bg-slate-300 px-4 font-semibold text-slate-700">Sync unavailable</button><p class="text-sm text-slate-600">Synchronization will be available after the importer is configured.</p></div></section>
